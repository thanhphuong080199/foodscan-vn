name: Build share APK on merge to main

on:
  push:
    branches: ['main']

jobs:
  build_android_preview:
    name: Build Android share APK (preview)
    type: build
    params:
      platform: android
      profile: preview
